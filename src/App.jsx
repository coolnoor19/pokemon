import { Pokemon } from "./Pokemon"

export const App = () => { // when we write export only then we have to access as object in {}, but if we write export default then we can access it by not using {}
  return (
    
      <Pokemon/>
    
  )
}